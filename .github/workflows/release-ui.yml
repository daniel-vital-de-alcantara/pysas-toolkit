name: Test and release PySAS Workbench

on:
  push:
    branches: [main]
    tags: ['v0.4.*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python -m pip install openpyxl
      - run: python -m unittest discover -s tests -v
      - run: node --check ui/app.js
      - run: node --test tests/test_ui.cjs
  release:
    needs: test
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: python tools/package_release.py
      - uses: softprops/action-gh-release@v2
        with:
          name: PySAS Workbench ${{ github.ref_name }}
          prerelease: ${{ contains(github.ref_name, 'preview') }}
          fail_on_unmatched_files: true
          body: |
            PySAS now has its own blue Windows taskbar icon and closes the terminal after successful startup.

            **Download PySAS-Workbench-Python-0.4.0-preview.8.zip below**, extract the entire ZIP and double-click START_PYSAS.bat.

            - START_PYSAS.bat opens an Edge/Chrome app window with its own profile. START_PYSAS_BROWSER.bat keeps normal browser mode available.
            - Separate taskbar identity per workspace: PySAS stays separate from Control Tree, and extracted versions can run side by side. Requires Python plus installed Edge or Chrome.
            - Closing the app stops new watcher intake and finishes active jobs before exiting. Browser mode has Quit app. START_PYSAS_CONSOLE.bat and .pysas-ui/launcher.log provide troubleshooting.
            - Fixed always_run: shared setup is prepended before EVERY scheduled program in the same SAS submission, including continuation. Definitions are not separate jobs.
            - Watcher _*.sas files (including .SAS and mixed-case extensions) remain shared initialization and are never queued as jobs. Shared-folder and inbox init files are prepended alphabetically; --lib retains its override behavior.
            - Files & folders: configure project inputs, shared initialization and watcher inbox. Upload multiple files directly, with progress and no replacement of existing files.
            - Keep PC awake prevents idle sleep/display timeout on Windows without generating clicks. Does not override manual sleep/lid-close or managed lock policies.
            - Live filenames, elapsed time, saved history, bundle folder shortcuts and blue styling remain included.
            - pysas.py 0.3.5 remains a standalone single-file CLI. Default paths and existing commands still work; optional --inbox and --init-dir select custom folders.

            Upgrade by copying all application files, including windows_app.py, launch_workbench.py and ui/icon.ico, into your existing workspace. Keep your SAS/EGP/workbook files and .pysas-ui settings.

            Requires Windows, Python 3.10+, configured SAS Enterprise Guide, and openpyxl for Excel features. No Node.js is needed. Automated tests cover lifecycle/scheduling with fake SAS jobs; real Enterprise Guide execution must be tested in your environment.
          files: |
            release/PySAS-Workbench-Python-*.zip
            release/SHA256SUMS.txt
